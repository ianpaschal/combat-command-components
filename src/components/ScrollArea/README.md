# Scroll Area

This component is directly copied from Combat Command without much alteration.
It still uses `radix-ui` instead of `base-ui` because their `ScrollArea` components have somewhat different APIs.
It still needs to be cleaned up and converted.
